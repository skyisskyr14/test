# 项目测试记录

> 记录时间：2026-03-22 02:58:04 UTC

## 1) 后端依赖与构建
### 命令
```bash
cd spring
mvn -q -DskipTests validate
mvn -q -DskipTests package
```

### 结果
- `validate` 成功通过。
- `package` 成功通过（生成可运行 jar）。

## 2) 前端依赖与构建
### 命令
```bash
cd web
npm install
npm run build
```

### 结果
- 依赖安装成功。
- `vite build` 成功，产物可生成。

## 3) Docker 编排脚本可执行性检查
### 命令
```bash
cd output
bash -n run.sh
bash -n stop.sh
```

### 结果
- 两个脚本语法检查通过。
- `run.sh` 已增强：等待 MySQL 就绪后再启动后端和前端，减少启动竞态失败。

## 4) 关键修复点回归
- 修复 MySQL 驱动坐标：`mysql:mysql-connector-j` -> `com.mysql:mysql-connector-j`。
- 后端启动命令增加 `-U`，避免 Maven 缓存错误坐标导致重复失败。

## 5) 服务联调建议（服务器执行）
```bash
cd output
./stop.sh
docker compose rm -f backend
./run.sh
docker compose logs -f backend
```
