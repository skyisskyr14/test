# 项目测试记录

> 记录时间：2026-03-22 02:58:04 UTC

## 1) 后端依赖与构建
### 命令
```bash
cd spring
mvn -q -DskipTests validate
mvn -q -DskipTests package
```

### 结果
- `validate` 成功通过。
- `package` 成功通过（生成可运行 jar）。

## 2) 前端依赖与构建
### 命令
```bash
cd web
npm install
npm run build
```

### 结果
- 依赖安装成功。
- `vite build` 成功，产物可生成。

## 3) Docker 编排脚本可执行性检查
### 命令
```bash
cd output
bash -n run.sh
bash -n stop.sh
```

### 结果
- 两个脚本语法检查通过。
- `run.sh` 已增强：等待 MySQL 就绪后再启动后端和前端，减少启动竞态失败。

## 4) 关键修复点回归
- 修复 MySQL 驱动坐标：`mysql:mysql-connector-j` -> `com.mysql:mysql-connector-j`。
- 后端启动命令增加 `-U`，避免 Maven 缓存错误坐标导致重复失败。

## 5) 服务联调建议（服务器执行）
```bash
cd output
./stop.sh
docker compose rm -f backend
./run.sh
docker compose logs -f backend
```


---

## 6) 本轮新增稳定性开发（2026-03-22 03:04:25 UTC）
- 新增后端健康检查接口：`/api/health` 与 `/api/health/db`。
- 登录页新增后端在线状态提示与重新检测按钮，减少“后端未启动仍尝试登录”的误操作。
- `run.sh`/`stop.sh` 脚本语法继续通过。

### 本轮复测命令
```bash
cd spring && mvn -q -DskipTests package
cd web && npm install && npm run build
cd output && bash -n run.sh && bash -n stop.sh
```

### 本轮复测结果
- 后端打包成功。
- 前端安装与构建成功。
- 启停脚本语法检查成功。


## 7) 角色前端分离与现代化界面开发（2026-03-22 03:11:17 UTC）
- 完成侧边栏菜单按角色动态显示（员工/HR/管理层）。
- 路由守卫按角色进行访问控制，禁止越权页面访问。
- 主控台首页按角色展示差异化卡片与待办。
- 登录页继续保持后端就绪检测机制。

### 复测命令
```bash
cd web && npm install && npm run build
cd spring && mvn -q -DskipTests package
cd output && bash -n run.sh && bash -n stop.sh
```

### 复测结果
- 前端构建成功。
- 后端打包成功。
- 启停脚本语法检查成功。


## 8) Browser/Screenshot 工具安装验证（2026-03-22 03:16:51 UTC）
### 命令
```bash
cd web
npm install -D playwright
npx playwright install chromium
npx playwright install-deps chromium
npm run screenshot -- https://example.com ../output/tool-check.png
```

### 结果
- Playwright 依赖安装成功。
- Chromium 浏览器与系统依赖安装成功。
- 截图命令执行成功，生成文件：`output/tool-check.png`。


## 9) 新界面开发与截图验证（2026-03-22 03:22:48 UTC）
### 变更
- 新增 `Roadmap` 项目进度看板页面。
- 菜单新增“项目进度”，并接入路由访问。
- 截图脚本支持通过环境变量注入登录态（`SEED_TOKEN` / `SEED_USER`）。

### 验证命令
```bash
cd web && npm run build
cd spring && mvn -q -DskipTests package
cd output && bash -n run.sh && bash -n stop.sh
cd web && npm run screenshot -- http://127.0.0.1:5173/ ../output/login-new.png
cd web && SEED_TOKEN=dummy SEED_USER='{"id":1,"username":"hr_admin","realName":"HR管理员","roleId":2}' npm run screenshot -- http://127.0.0.1:5173/dashboard/roadmap ../output/roadmap-new.png
```

### 结果
- 前后端构建通过。
- 新页面截图产物生成成功：`output/login-new.png`、`output/roadmap-new.png`。
